<view class="page-body">
  <view class="page-section page-section-gap">
    <map id="myMap" style="width: 100%; height: 100%;position:fixed;top:0;" latitude="{{latitude}}" longitude="{{longitude}}" markers="{{markers}}" covers="{{covers}}" show-location></map>
  </view>
  <view class="absolute_box">
    <view class="play_ticke">立即打卡</view>
  </view>
</view>